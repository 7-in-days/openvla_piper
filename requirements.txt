# Compatibility pointer. Supported Ada GPU dependencies are pinned in:
#   requirements-openvla-ada.txt
#   constraints-openvla-ada.txt
# Run scripts/install_openvla.sh instead of installing this file directly.
-r requirements-openvla-ada.txt
